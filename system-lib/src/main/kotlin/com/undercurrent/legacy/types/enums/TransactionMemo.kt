package com.undercurrent.legacy.types.enums

enum class TransactionMemo {
    //todo include this in data migration
    TO_VENDOR,
    TO_ADMIN1,
    TO_ADMIN2,
    FROM_CUSTOMER,
    MARKED_PAID,
    CASHOUT,
    SWAP,
}