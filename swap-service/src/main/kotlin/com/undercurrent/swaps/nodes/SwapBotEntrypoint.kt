package com.undercurrent.swaps.nodes


/**
 * Start implementing the lifecycle from the swap-service module
 */
//class SwapBotEntrypoint(sessionContext: SessionContext) :
//    Executable(CmdRegistry.STARTSWAP, sessionContext) {
//
//    override suspend fun execute() {
//
////        SwapBotBridgeNode(sessionContext).executeAll()
//
//        //will want to pass current user context in (not role, necessarily)
////        SwapBotStart(sessionPair.user, sessionPair.role, RunConfig.envMode).start(user = sessionPair.user, )
//
//
//
//
//
//    }
//
//
//}